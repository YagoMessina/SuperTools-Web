const home = "/";
const files = "/files";
const notes = "/notes";
const note = "/note";
const noteCreation = "/note/create";

const path = {
  home,
  files,
  notes,
  note,
  noteCreation,
};

export default path;
